import Carousel from "../../components/UI/carousel/Carousel";

const Tour = () => {
  return (
    <div className="min-h-screen bg-zinc-200 flex flex-col items-center justify-center">
      <h2 className="text-black ">Check Out Our Space!</h2>
      <Carousel />
    </div>
  );
};

export default Tour;
